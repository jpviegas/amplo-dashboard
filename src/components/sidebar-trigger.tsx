import { SidebarTrigger } from "@/components/ui/sidebar";

export function CustomTrigger() {
  //   const { toggleSidebar } = useSidebar();

  return (
    <SidebarTrigger />
    // <Button size="icon" variant="outline" onClick={toggleSidebar}>

    //   <GoSidebarCollapse className="h-[1.2rem] w-[1.2rem] rotate-0 scale-100 transition-all dark:-rotate-90 dark:scale-0" />
    //   <GoSidebarExpand className="absolute h-[1.2rem] w-[1.2rem] rotate-90 scale-0 transition-all dark:rotate-0 dark:scale-100" />
    //   <span className="sr-only">Toggle sidebar</span>
    // </Button>
  );
}
